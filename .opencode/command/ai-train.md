---
description: Train the spaceAI ML models (run.py train)
---

Run `npm run ai:train` and report the resulting accuracy/metrics. If the user mentions corrections, run `npm run ai:retrain` instead. $ARGUMENTS passes extra args to the training run.
