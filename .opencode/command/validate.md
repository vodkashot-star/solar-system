---
description: Run the full validation suite (tsc + vitest)
---

Run `npm run validate`. Fix any type errors or failing tests. If the GLB models were changed recently, also run `npm run models:validate`.
