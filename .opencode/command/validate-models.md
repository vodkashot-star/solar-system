---
description: Validate all GLB models (headers, asset JSONs, Draco, sizes)
---

Run `npm run models:validate`. If failures are reported, run `npm run models:validate:fix` to auto-fix (Draco compress), then re-validate. $ARGUMENTS may be `--json` for CI output.
