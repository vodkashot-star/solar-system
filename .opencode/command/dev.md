---
description: Start the full dev stack (Express :5000 + FastAPI :8000)
---

Run `npm run dev`. Wait for both servers (Express on :5000, FastAPI on :8000) to report ready before reporting success. If 3D models fail to load afterwards, run `bash scripts/copy-draco.sh` first.
